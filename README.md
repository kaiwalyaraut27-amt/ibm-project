# Simple Interest Calculator

This project contains a shell script that calculates simple interest.

Formula used:

Simple Interest = (Principal × Rate × Time) / 100

The script asks the user to enter the principal amount, rate of interest, and time. It then calculates and displays the simple interest.

This project demonstrates the use of GitHub repositories, version control, and collaboration practices.
